name: Test Actions

on:
  push :
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    if : github.ref_name == 'man'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run tests
        run: |
          echo "Works"